#print(f"Dados: {img[:10]}...")  # Print first 10 pixel values for brevity
#print(f"Image data length after unpacking: {len(img)}")

#sim.saveImage(img, resolution, 0, '/home/kelton/Documentos/GitHub/EdgeBoxAI/controller/camera_image3.png', -1)

cv2.imshow("Imagem", img)
cv2.waitKey(0)
cv2.destroyAllWindows()

caixa = sim.getObjectHandle('Cuboid0')
nova_cor = [0.0, 0.0, 1.0]
sim.setShapeColor(caixa, None, sim.colorcomponent_ambient_diffuse, nova_cor)

img = cv2.resize(img, (256, 256))

plt.imshow(img)
plt.axis('off')
plt.tight_layout(pad=0)
plt.savefig('minha_imagem_rgb_salva.png', 
            bbox_inches='tight', # Garante que nada fora da imagem seja salvo
            pad_inches=0,        # Remove qualquer margem extra
            dpi=300)